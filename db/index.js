/* SELECT 
	department.id, 
 department.name
FROM 
	department */
/* SELECT
	role.department_id,
   role.title,
    role.salary
  FROM role; */
/* SELECT 
	employee.first_name,
    employee.last_name,
    employee.role_id,
    employee.manager_id
FROM employee;  */